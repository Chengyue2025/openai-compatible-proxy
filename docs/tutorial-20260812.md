# AI API使用教程：Python快速接入

# Python快速接入教程

## 安装OpenAI SDK

```bash
pip install openai
```

## 基础调用

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://116.204.91.171:3000/v1",
    api_key="你的key"
)

response = client.chat.completions.create(
    model="glm-4-flash",
    messages=[
        {"role": "system", "content": "你是一个有用的助手"},
        {"role": "user", "content": "介绍一下你自己"}
    ],
    temperature=0.7
)

print(response.choices[0].message.content)
```

## 流式输出（Streaming）

```python
stream = client.chat.completions.create(
    model="glm-4-flash",
    messages=[{"role": "user", "content": "写一首诗"}],
    stream=True
)

for chunk in stream:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="")
```

## 获取Token额度

```python
# 查看剩余额度
response = client.models.list()
print(response)
```

## 支持模型

| 模型 | 特点 |
|------|------|
| glm-4-flash | 快速响应，免费 |
| glm-4-plus | 高质量，复杂任务 |
| glm-4-air | 轻量级，低成本 |
| llama-3.1-8b-instruct | 开源模型，本地化部署经验 |

## 购买API Key

👉 [立即购买](https://chengyue2025.github.io/low-cost-ai-api/)

---
*本文由AI自动生成，仅供学习参考*
